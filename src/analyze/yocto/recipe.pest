char = { ASCII_ALPHANUMERIC | ":" | "/" | "." | "-" | "$" | "{" | "}" | "_"}
WHITESPACE = _{ " " | "\n" | "\t" }
uri = @{ char+ }
src_uris = { "SRC_URI" ~ "=" ~ "\"" ~ uri ~ ("\\"? ~ uri ~ "\\"?)* ~ "\""  }