#!/bin/bash
realpath=`readlink -fn $0`
realdir=`dirname $realpath`
export GDK_PIXBUF_MODULE_FILE=$realdir/../../usr/lib/gdk-pixbuf-2.0/2.10.0/loaders.cache
exec -a "$0" $realdir/gtk-update-icon-cache.real "$@"
